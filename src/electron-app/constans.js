const NAME_PROJECT = 'Time Tracking'
const AUTH_DATA = 'auth_data'
const AUTH_PLUGIN_DATA = 'auth_plugin_data'
const AUTH_REMEMBER_DATA = 'remember_data'
const OAUTH2 = 'OAuth2'
const BASIC_AUTH = 'Basic auth'

const PLUGINS = {
    TEMPO: 'TEMPO',
}

module.exports = { NAME_PROJECT, AUTH_DATA, BASIC_AUTH, OAUTH2, AUTH_REMEMBER_DATA, AUTH_PLUGIN_DATA, PLUGINS }
