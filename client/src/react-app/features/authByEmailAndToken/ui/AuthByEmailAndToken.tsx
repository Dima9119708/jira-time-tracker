interface AuthByEmailAndTokenProps {
    className?: string
}

const AuthByEmailAndToken = (props: AuthByEmailAndTokenProps) => {
    const {} = props

    return <div>sacsac</div>
}

export default AuthByEmailAndToken
